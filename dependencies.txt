installed dependencies:
npm install libraryName --save = this downloads the library and saves it to the folder specified, not globally.

npm install nodemon --save-dev = the -dev indicates that the package is only installed for the development.

    "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongoose": "^7.3.3",
    "multer": "^1.4.5-lts.1",
    "validator": "^13.9.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }

nodemon
"scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js" (here, you can use node index.js to start or nodemon) if you use node ,the script will be use only once but if using nodemon , the script will be live waiting for changes to be applied.
    npm start is the command
    so change them as needed 
  },

  JSON FILE IS NODEMON SO RUN = npm start to see changes live.
  otherwise just use node index.js